import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sis.studentinformationsystem',
  appName: 'Student Information System',
  webDir: 'dist'
};

export default config;
